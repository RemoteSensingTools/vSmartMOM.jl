import{_ as a,o as t,c as o,aB as s}from"./chunks/framework.C7rtsiTl.js";const u=JSON.parse('{"title":"Run on GPU","description":"","frontmatter":{},"headers":[],"relativePath":"pages/gpu.md","filePath":"pages/gpu.md","lastUpdated":null}'),i={name:"pages/gpu.md"};function r(c,e,n,l,d,h){return t(),o("div",null,[...e[0]||(e[0]=[s(`<h1 id="Run-on-GPU" tabindex="-1">Run on GPU <a class="header-anchor" href="#Run-on-GPU" aria-label="Permalink to &quot;Run on GPU {#Run-on-GPU}&quot;">​</a></h1><p><strong>For:</strong> users who want to run the solver on GPU backends.</p><p><strong>Next:</strong> <a href="./quickstart">Quick Start</a>, <a href="./IO/Overview">IO API</a>, <a href="./concepts/07_architecture">Architecture-Agnostic Code (Concepts)</a>.</p><p>Every RT array in vSmartMOM has shape <code>(NquadN, NquadN, nSpec)</code> — the spectral axis is the batch axis, and the elemental/doubling/interaction kernels run as one batched matrix operation per layer per Fourier moment. GPU support attaches to that same kernel source via <a href="https://juliagpu.github.io/KernelAbstractions.jl" target="_blank" rel="noreferrer">KernelAbstractions.jl</a> and Julia package extensions, with no second code path. See <a href="./concepts/07_architecture">Architecture-Agnostic Code (Concepts)</a> for the full story with file:line evidence; this page is the practical how-to.</p><h2 id="Selecting-an-architecture" tabindex="-1">Selecting an architecture <a class="header-anchor" href="#Selecting-an-architecture" aria-label="Permalink to &quot;Selecting an architecture {#Selecting-an-architecture}&quot;">​</a></h2><p>Set <code>radiative_transfer.architecture</code> in the scene configuration:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">radiative_transfer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  architecture</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Architectures.CPU()</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       # default; always available</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # architecture: Architectures.GPU()       # CUDA</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # architecture: Architectures.MetalGPU()  # Apple Silicon</span></span></code></pre></div><p>or override an already-loaded <code>vSmartMOM_Parameters</code> in a script:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">params</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">architecture </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> vSmartMOM</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Architectures</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">GPU</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span></code></pre></div><p><code>default_architecture()</code> picks <code>GPU()</code> if a functional CUDA device was detected when the CUDA extension loaded, <code>MetalGPU()</code> if Metal was detected, and <code>CPU()</code> otherwise; it is used when <code>architecture</code> is left unset.</p><h2 id="CUDA-vs-Metal" tabindex="-1">CUDA vs Metal <a class="header-anchor" href="#CUDA-vs-Metal" aria-label="Permalink to &quot;CUDA vs Metal {#CUDA-vs-Metal}&quot;">​</a></h2><p>Both backends attach through Julia package extensions (<code>ext/vSmartMOMCUDAExt.jl</code>, <code>ext/vSmartMOMMetalExt.jl</code>): the dispatch methods <code>Architectures.devi</code>, <code>Architectures.array_type</code>, and <code>Architectures.architecture</code> are only defined for <code>GPU()</code> / <code>MetalGPU()</code> once the matching backend package has been loaded in the same session (<code>using CUDA</code> or <code>using Metal</code>). Calling <code>GPU()</code>/<code>MetalGPU()</code> without the matching package loaded throws an actionable error (<code>Architectures.ka_backend</code>, <code>default_mie_precision_policy</code>) rather than failing inside a kernel launch.</p><ul><li><p><strong>CUDA (<code>GPU()</code>)</strong> is the mature, most-tested path: CUBLAS-backed batched matmul/inverse, a GPU Mie pipeline (NAI2), and ForwardDiff <code>Dual</code> support through the batched-matmul kernels for linearized (Jacobian) runs.</p></li><li><p><strong>Metal (<code>MetalGPU()</code>)</strong> is a first-pass Apple Silicon path: a portable KernelAbstractions matmul + LU-with-partial-pivoting kernel, no vendor BLAS. The batched-inverse kernel uses <code>@localmem</code> threadgroup memory with a conservative 32 KiB guard, so <code>Float32</code> matrices with <code>N = Nquad * n_stokes ≥ 64</code> are rejected with a clear local-memory error instead of a driver crash.</p></li></ul><h2 id="Float32-vs-Float64" tabindex="-1">Float32 vs Float64 <a class="header-anchor" href="#Float32-vs-Float64" aria-label="Permalink to &quot;Float32 vs Float64 {#Float32-vs-Float64}&quot;">​</a></h2><ul><li><p><code>MetalGPU()</code> requires <code>Float32</code> scene parameters (<code>float_type: Float32</code>).</p></li><li><p><code>GPU()</code> (CUDA) supports both. The GPU Mie precision policy auto-selects <code>NativeFloat64</code> for <code>Float64</code> models and a Float32-native double-single path (<code>DSEmulated</code>) for <code>Float32</code> models (<code>default_mie_precision_policy</code>).</p></li><li><p><code>CPU()</code> supports both, with no precision-policy distinction.</p></li></ul><h2 id="What-is-GPU-safe-today" tabindex="-1">What is GPU-safe today <a class="header-anchor" href="#What-is-GPU-safe-today" aria-label="Permalink to &quot;What is GPU-safe today {#What-is-GPU-safe-today}&quot;">​</a></h2><ul><li><p><strong>Forward elastic RT</strong> (elemental → doubling → interaction, <code>noRS</code>) — fully GPU-friendly on CUDA; this is the hot path the batched design targets.</p></li><li><p><strong>Linearized RT (Jacobians)</strong> — GPU-friendly on CUDA (<code>test/local/gpu/test_jacobians_GPU.jl</code>); Metal Jacobian workflows have not been validated yet, so use <code>CPU()</code> or <code>GPU()</code> for linearized runs.</p></li><li><p><strong>Raman (RRS/VS)</strong> — runs on GPU (<code>test/local/gpu/test_forward_raman_gpu.jl</code>) but carries materially higher memory pressure than the elastic path — the 4-D cross-wavelength coupling arrays scale with <code>nSpec * n_Raman</code> — so watch VRAM on large spectral batches.</p></li><li><p><strong>Atmosphere/surface split caches</strong> (<code>rt_run_atmosphere</code> / <code>rt_run_surface</code>, see <a href="./batch_processing">Fast Re-runs &amp; Batch Processing</a>) keep their <code>AtmosphereRTCache</code> snapshot device-resident: the cached R/T/J arrays and <code>τ_sum_surf</code> stay on the architecture the cache was built on, so a GPU-built cache replays on GPU without a host round-trip.</p></li><li><p><strong>Mie scattering</strong> — automatic GPU path on CUDA (<code>make_mie_model(...; architecture=GPU())</code>, NAI2 only; PCW and the ForwardDiff AD path fall back to CPU). Always CPU on Metal — faster than the Metal multi-kernel path for typical aerosol loads anyway.</p></li><li><p><strong>Gas absorption</strong> — the production pipeline (<code>model_from_parameters</code> → <code>rt_run</code>) computes line-by-line absorption via <a href="https://github.com/RemoteSensingTools/AtmosphericAbsorption.jl" target="_blank" rel="noreferrer">AtmosphericAbsorption.jl</a>, which has its own backend dispatch; the standalone <code>Absorption</code> module also ships a GPU Voigt kernel for direct σ(ν,T,p) queries.</p></li></ul><h2 id="CPU-fallback" tabindex="-1">CPU fallback <a class="header-anchor" href="#CPU-fallback" aria-label="Permalink to &quot;CPU fallback {#CPU-fallback}&quot;">​</a></h2><p><code>CPU()</code> is always available and is the safe default for debugging scenes and for any workflow not yet validated on a GPU backend (Metal Jacobians). Pass <code>architecture=CPU()</code> to <code>make_mie_model</code> to force CPU Mie regardless of the RT solver&#39;s own architecture. The local GPU test suite (<code>test/local/gpu/</code>, not part of CI) self-skips cleanly when no CUDA device is present.</p><h2 id="Performance-Notes" tabindex="-1">Performance Notes <a class="header-anchor" href="#Performance-Notes" aria-label="Permalink to &quot;Performance Notes {#Performance-Notes}&quot;">​</a></h2><p>The main architecture switch is the <code>radiative_transfer.architecture</code> field in the scene configuration, or <code>params.architecture = GPU()</code> after loading a configuration. GPU runs are most useful when enough spectral points, layers, or viewing geometries are batched to amortize kernel-launch and transfer costs. Use <code>CPU()</code> for small debugging scenes and for workflows that are not yet GPU-safe.</p><p>For Apple Silicon experiments, load Metal.jl and use <code>MetalGPU()</code> with <code>Float32</code> scene parameters. The current Metal path focuses on the core batched matrix multiply and inverse operations; fused kernels can be added after Mac validation. The portable inverse kernel uses Metal threadgroup memory and is intended for modest stream/Stokes dimensions; larger matrices fail early with a clear local-memory error instead of a driver launch failure. With the current 32 KiB guard, Float32 matrices with <code>N = Nquad * nStokes &gt;= 64</code> are rejected. Metal Jacobian workflows have not been validated yet, so use CPU or CUDA for linearized runs.</p>`,22)])])}const g=a(i,[["render",r]]);export{u as __pageData,g as default};

import{_ as t,o as a,c as o,aB as r}from"./chunks/framework.C7rtsiTl.js";const u=JSON.parse('{"title":"Run on GPU","description":"","frontmatter":{},"headers":[],"relativePath":"pages/gpu.md","filePath":"pages/gpu.md","lastUpdated":null}'),i={name:"pages/gpu.md"};function s(n,e,d,c,l,h){return a(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="Run-on-GPU" tabindex="-1">Run on GPU <a class="header-anchor" href="#Run-on-GPU" aria-label="Permalink to &quot;Run on GPU {#Run-on-GPU}&quot;">​</a></h1><p><strong>For:</strong> users who want to run the solver on GPU backends.</p><p><strong>Next:</strong> <a href="./quickstart">Quick Start</a>, <a href="./IO/Overview">IO API</a>, <a href="./concepts/07_architecture">Architecture-Agnostic Code (Concepts)</a>.</p><p>Every RT array in vSmartMOM has shape <code>(NquadN, NquadN, nSpec)</code> — the spectral axis is the batch axis, and the elemental/doubling/interaction kernels run as one batched matrix operation per layer per Fourier moment. GPU support attaches to that same kernel source via <a href="https://juliagpu.github.io/KernelAbstractions.jl" target="_blank" rel="noreferrer">KernelAbstractions.jl</a> and Julia package extensions, with no second code path. See <a href="./concepts/07_architecture">Architecture-Agnostic Code (Concepts)</a> for the full story with file:line evidence; this page is the practical how-to.</p><h2 id="Selecting-an-architecture" tabindex="-1">Selecting an architecture <a class="header-anchor" href="#Selecting-an-architecture" aria-label="Permalink to &quot;Selecting an architecture {#Selecting-an-architecture}&quot;">​</a></h2><p>Set <code>radiative_transfer.architecture</code> in the scene configuration:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">radiative_transfer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  architecture</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Architectures.CPU()</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       # default; always available</span></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # architecture: Architectures.GPU()       # CUDA</span></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # architecture: Architectures.MetalGPU()  # Apple Silicon</span></span></code></pre></div><p>or override an already-loaded <code>vSmartMOM_Parameters</code> in a script:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">params</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">architecture </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> vSmartMOM</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Architectures</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">GPU</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span></code></pre></div><p><code>default_architecture()</code> picks <code>GPU()</code> if a functional CUDA device was detected when the CUDA extension loaded, <code>MetalGPU()</code> if Metal was detected, and <code>CPU()</code> otherwise; it is used when <code>architecture</code> is left unset.</p><h2 id="CUDA-vs-Metal" tabindex="-1">CUDA vs Metal <a class="header-anchor" href="#CUDA-vs-Metal" aria-label="Permalink to &quot;CUDA vs Metal {#CUDA-vs-Metal}&quot;">​</a></h2><p>Both backends attach through Julia package extensions (<code>ext/vSmartMOMCUDAExt.jl</code>, <code>ext/vSmartMOMMetalExt.jl</code>): the dispatch methods <code>Architectures.devi</code>, <code>Architectures.array_type</code>, and <code>Architectures.architecture</code> are only defined for <code>GPU()</code> / <code>MetalGPU()</code> once the matching backend package has been loaded in the same session (<code>using CUDA</code> or <code>using Metal</code>). Calling <code>GPU()</code>/<code>MetalGPU()</code> without the matching package loaded throws an actionable error (<code>Architectures.ka_backend</code>, <code>default_mie_precision_policy</code>) rather than failing inside a kernel launch.</p><ul><li><p><strong>CUDA (<code>GPU()</code>)</strong> is the mature, most-tested path: CUBLAS-backed batched matmul/inverse, a GPU Mie pipeline (NAI2), and ForwardDiff <code>Dual</code> support through the batched-matmul kernels for linearized (Jacobian) runs.</p></li><li><p><strong>Metal (<code>MetalGPU()</code>)</strong> is a first-pass Apple Silicon path: a portable KernelAbstractions matmul + LU-with-partial-pivoting kernel, no vendor BLAS. The batched-inverse kernel uses <code>@localmem</code> threadgroup memory with a conservative 32 KiB guard, so <code>Float32</code> matrices with <code>N = Nquad * n_stokes ≥ 64</code> are rejected with a clear local-memory error instead of a driver crash.</p></li></ul><h2 id="Float32-vs-Float64" tabindex="-1">Float32 vs Float64 <a class="header-anchor" href="#Float32-vs-Float64" aria-label="Permalink to &quot;Float32 vs Float64 {#Float32-vs-Float64}&quot;">​</a></h2><ul><li><p><code>MetalGPU()</code> requires <code>Float32</code> scene parameters (<code>float_type: Float32</code>) — Metal has no Float64 hardware support at all. <code>default_mie_precision_policy</code> auto-selects <code>NativeFloat32</code> (pure Float32, never Float64-widened) for Float32 inputs on Metal, and throws a clear <code>ArgumentError</code> for Float64 inputs rather than silently failing inside a device-array allocation.</p></li><li><p><code>GPU()</code> (CUDA) supports both. The GPU Mie precision policy auto-selects <code>NativeFloat64</code> for <code>Float64</code> models and a Float32-native double-single path (<code>DSEmulated</code>) for <code>Float32</code> models (<code>default_mie_precision_policy</code>). Consumer/F32-throughput-limited CUDA GPUs can opt into <code>NativeFloat32</code> explicitly (<code>precision_policy = Scattering.NativeFloat32()</code>) for the caller-node Mie API (<code>compute_aerosol_optical_properties_nodes</code>/ <code>compute_aerosol_extinction_nodes</code>) — end-to-end Float32, zero Float64 device arrays, at a further accuracy cost vs <code>DSEmulated</code> (see <code>vSmartMOM.Scattering.MiePrecisionPolicy</code> and <code>test/test_mie_nodes.jl</code> for measured error bands).</p></li><li><p><code>CPU()</code> supports both, with no precision-policy distinction.</p></li></ul><h3 id="NativeFloat32-measured-accuracy-caller-node-Mie-API,-vs-Float64-CPU-reference" tabindex="-1"><code>NativeFloat32</code> measured accuracy (caller-node Mie API, vs Float64 CPU reference) <a class="header-anchor" href="#NativeFloat32-measured-accuracy-caller-node-Mie-API,-vs-Float64-CPU-reference" aria-label="Permalink to &quot;`NativeFloat32` measured accuracy (caller-node Mie API, vs Float64 CPU reference) {#NativeFloat32-measured-accuracy-caller-node-Mie-API,-vs-Float64-CPU-reference}&quot;">​</a></h3><p>Measured on this host (A100, <code>test/test_mie_nodes.jl</code>), KA-CPU backend, standard log-normal set (nquad=300) and a wide-range (0.005-6 μm) TOMAS-like set:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Metric</th><th style="text-align:right;">standard set</th><th style="text-align:right;">wide TOMAS set</th></tr></thead><tbody><tr><td style="text-align:right;"><code>k</code> (extinction) relerr</td><td style="text-align:right;">8.1e-8</td><td style="text-align:right;">7.3e-9</td></tr><tr><td style="text-align:right;"><code>ω̃</code> (SSA) relerr</td><td style="text-align:right;">7.8e-8</td><td style="text-align:right;">1.9e-8</td></tr><tr><td style="text-align:right;">Greek coefficients, max abs (α/β/δ/ζ worst)</td><td style="text-align:right;">5.0e-3</td><td style="text-align:right;">4.3e-3</td></tr><tr><td style="text-align:right;">P11 (reconstructed phase function), max relerr</td><td style="text-align:right;">0.89%</td><td style="text-align:right;">0.19%</td></tr><tr><td style="text-align:right;">P12/P11, raw max abs</td><td style="text-align:right;">1.6e-4</td><td style="text-align:right;">2.5e-4</td></tr><tr><td style="text-align:right;">P33/P11, raw max abs</td><td style="text-align:right;"><strong>1.4%</strong></td><td style="text-align:right;"><strong>3.8%</strong></td></tr><tr><td style="text-align:right;">P34/P11, raw max abs</td><td style="text-align:right;">3.1e-4</td><td style="text-align:right;">2.8e-4</td></tr><tr><td style="text-align:right;">P12/P11, P11-weighted RMS</td><td style="text-align:right;">6.6e-6</td><td style="text-align:right;">6.1e-6</td></tr><tr><td style="text-align:right;">P33/P11, P11-weighted RMS</td><td style="text-align:right;">3.3e-4</td><td style="text-align:right;">7.8e-4</td></tr><tr><td style="text-align:right;">P34/P11, P11-weighted RMS</td><td style="text-align:right;">1.5e-5</td><td style="text-align:right;">9.8e-6</td></tr><tr><td style="text-align:right;">|Δf₃₃| / peak(P11)</td><td style="text-align:right;">5.5e-3</td><td style="text-align:right;">9.2e-5</td></tr></tbody></table><p><code>k</code>/<code>ω̃</code> land close to <code>DSEmulated</code>&#39;s established floor (both are dominated by Neumaier-compensated summation, not the Dₙ recursion). Greek coefficients and the P33/P11 polarized ratio are the clearest signal of <code>NativeFloat32</code>&#39;s reduced accuracy vs <code>DSEmulated</code> (no double-single Dₙ emulation, and — unlike the log-normal <code>MieModel</code> GPU path below — the reduction itself is never Float64-widened, by design, to keep zero Float64 device arrays). P12/P11 and P34/P11 stay small throughout. The RAW max ratio errors above are dominated by grid angles where P11 is tiny (near-null scattering angles — physically negligible, numerically fragile as a RATIO denominator); the P11-WEIGHTED RMS and peak-normalized absolute-element rows are the physically meaningful companions (weight ∝ how many photons actually scatter at that angle) and are 1-2 orders of magnitude tighter than the raw max in every case. See <code>test/test_mie_nodes.jl</code>&#39;s &quot;NativeFloat32: accuracy...&quot; and &quot;NativeFloat32: polarized phase-matrix validation gate&quot; testsets for the exact reproduction recipe.</p><p>On the log-normal <code>MieModel</code> GPU path (<code>compute_aerosol_optical_properties_gpu</code>), <code>NativeFloat32</code> lands at <code>k</code>/<code>ω̃</code> relerr ~1-2e-7 and Greek coefficients ~7e-5 abs — much closer to <code>NativeFloat64</code> than to the caller-node path&#39;s numbers above — because that path&#39;s host-side size-distribution reduction is ALWAYS Float64-widened regardless of precision policy (see <code>test/local/gpu/test_mie_gpu.jl</code>&#39;s &quot;NativeFloat32 (log-normal MieModel GPU path)&quot; testsets).</p><h2 id="What-is-GPU-safe-today" tabindex="-1">What is GPU-safe today <a class="header-anchor" href="#What-is-GPU-safe-today" aria-label="Permalink to &quot;What is GPU-safe today {#What-is-GPU-safe-today}&quot;">​</a></h2><ul><li><p><strong>Forward elastic RT</strong> (elemental → doubling → interaction, <code>noRS</code>) — fully GPU-friendly on CUDA; this is the hot path the batched design targets.</p></li><li><p><strong>Linearized RT (Jacobians)</strong> — GPU-friendly on CUDA (<code>test/local/gpu/test_jacobians_GPU.jl</code>); Metal Jacobian workflows have not been validated yet, so use <code>CPU()</code> or <code>GPU()</code> for linearized runs.</p></li><li><p><strong>Raman (RRS/VS)</strong> — runs on GPU (<code>test/local/gpu/test_forward_raman_gpu.jl</code>) but carries materially higher memory pressure than the elastic path — the 4-D cross-wavelength coupling arrays scale with <code>nSpec * n_Raman</code> — so watch VRAM on large spectral batches.</p></li><li><p><strong>Atmosphere/surface split caches</strong> (<code>rt_run_atmosphere</code> / <code>rt_run_surface</code>, see <a href="./batch_processing">Fast Re-runs &amp; Batch Processing</a>) keep their <code>AtmosphereRTCache</code> snapshot device-resident: the cached R/T/J arrays and <code>τ_sum_surf</code> stay on the architecture the cache was built on, so a GPU-built cache replays on GPU without a host round-trip.</p></li><li><p><strong>Mie scattering</strong> — automatic GPU path on CUDA (<code>make_mie_model(...; architecture=GPU())</code>, NAI2 only; PCW and the ForwardDiff AD path fall back to CPU). <code>MetalGPU()</code> now also has a registered GPU Mie pipeline (<code>has_gpu_mie(::MetalGPU) = true</code>, Float32-only, <code>NativeFloat32</code>); <code>has_gpu_mie</code> is a single architecture-level trait shared by the log-normal <code>MieModel</code> GPU path AND the caller-node Mie API, so both route through Metal — and BOTH are <code>NativeFloat32</code>-aware end to end (shared <code>_mie_kernel1</code> Kernel-1 dispatch, shared Metal-only-Float64-device-array guard). The caller-node Mie API (<code>compute_aerosol_optical_properties_nodes</code>/ <code>compute_aerosol_extinction_nodes</code>) is the primary target of the Metal Mie route (device-resident reduction, so its <code>RA</code> reduction type must never be Float64 under <code>NativeFloat32</code>); the log-normal path&#39;s reduction is host-side regardless of policy, so it was never at risk of an illegal Float64 device array in the first place.</p></li><li><p><strong>Gas absorption</strong> — the production pipeline (<code>model_from_parameters</code> → <code>rt_run</code>) computes line-by-line absorption via <a href="https://github.com/RemoteSensingTools/AtmosphericAbsorption.jl" target="_blank" rel="noreferrer">AtmosphericAbsorption.jl</a>, the sole supported absorption dependency going forward, with its own backend dispatch. The internal standalone <code>Absorption</code> module is legacy-only and will be removed; do not use its GPU Voigt kernel for new work.</p></li></ul><h2 id="Batched-caller-node-Mie-seam-exact-nmax-grouping" tabindex="-1">Batched caller-node Mie seam (exact-<code>nmax</code> grouping) <a class="header-anchor" href="#Batched-caller-node-Mie-seam-exact-nmax-grouping" aria-label="Permalink to &quot;Batched caller-node Mie seam (exact-`nmax` grouping) {#Batched-caller-node-Mie-seam-exact-nmax-grouping}&quot;">​</a></h2><p>For many-ensemble columns (e.g. GCHPIO&#39;s per-layer TOMAS size-distribution inventory: hundreds of ensembles × tens of nodes each, one ensemble per aerosol population per layer), launching one GPU Mie call per ensemble pays kernel-launch overhead hundreds of times per spectral point. The batched seam (<code>prepare_mie_node_geometry</code> + <code>compute_aerosol_optical_properties_nodes_batched</code> / <code>compute_aerosol_extinction_nodes_batched</code>, see <a href="/vSmartMOM.jl/previews/PR230/pages/api/scattering#Batched-Caller-Node-Mie-Seam">Batched Caller-Node Mie Seam</a> and <code>proposals/batched_mie_nodes_seam.md</code>) groups ensembles by their EXACT Mie <code>nmax</code> term count at the current wavelength and issues one kernel-1 launch per group, instead of per ensemble or a single global-max-padded launch (the padded design was rejected: it breaks bit-identity with the single-ensemble reference and inflates memory ~8× on the real GCHPIO column). Segmented, order-preserving reductions then recombine each group&#39;s per-node results into per-ensemble bulk optics, and results are reassembled in the caller&#39;s original ensemble order.</p><p><strong>Public bit-compatibility contract (precise, by backend):</strong></p><ul><li><p><strong>CPU (<code>Architectures.CPU()</code>) and KA-CPU (any KernelAbstractions backend that isn&#39;t real CUDA/Metal hardware)</strong>: the batched path is <strong>bit-for-bit identical</strong> (<code>==</code>) to calling <code>compute_aerosol_optical_properties_nodes</code>/<code>compute_aerosol_extinction_nodes</code> once per ensemble — by construction (same arithmetic order and the same per-ensemble normalize-once semantics, just re-grouped into batched kernel launches / a <code>Threads.@threads</code> loop over ensembles on CPU). Verified for all three precision policies, full and extinction-only variants, mixed-RI and mixed-size fixtures, AND — as of this seam&#39;s Float32-output-contract fix — Float32 CPU inputs/outputs specifically (not just Float64).</p></li><li><p><strong>Real CUDA hardware</strong>: batched-vs-single agree to <strong>near machine precision, NOT bit-for-bit</strong>, because the two code paths launch kernels over different <code>ndrange</code> shapes (grouped-batch size vs single-ensemble size) and CUDA&#39;s compiler is free to generate different PTX/SASS (loop unrolling, FMA contraction, register allocation) for logically identical, race-free source code compiled for a different launch configuration — a well-known GPU characteristic, not a logic bug (confirmed by exhaustive same-shape manual verification finding zero divergence at every intermediate stage). Measured on an A100: <code>k</code>/<code>ω̃</code> relative errors ~1e-16 to ~1.4e-7 and Greek-coefficient absolute differences ~1e-15 to ~8e-6 depending on precision policy, consistent with this codebase&#39;s existing KA-CPU-vs-real-CUDA tolerances elsewhere. See <code>test/test_mie_nodes_batched.jl</code>&#39;s <code>_run_bitwise_gate</code> docstring for the exact numbers and the <code>exact=false</code> real-CUDA tolerance rationale.</p></li><li><p><strong>The one invariant that IS exact (<code>==</code>) even on real CUDA, for all three precision policies</strong>: <code>compute_aerosol_extinction_nodes_batched[_gpu]</code>&#39;s result for ensemble <code>e</code> equals <code>compute_aerosol_optical_properties_nodes_batched[_gpu]</code>&#39;s <code>.k</code> for that SAME ensemble — the full and extinction-only batched kernels share the identical Kernel-1 + cross-section + segmented-weighted-sum code path (extinction-only just stops there instead of continuing into amplitude/phase/Greek), so this invariant does not depend on the launch-shape-sensitive downstream reduction at all. This is the exact invariant GCHPIO&#39;s hybrid exact-τ mechanism (extinction-only for most of the spectrum, full optics where Greek coefficients are actually needed) rests on; it has its own dedicated strict (zero-tolerance <code>==</code>) real-CUDA regression testset (<code>test_mie_nodes_batched.jl</code>&#39;s &quot;Gate 1 (CUDA, STRICT exact): full-batched .k == extinction-only-batched k&quot;) rather than being folded into the tolerance-based comparisons above.</p></li></ul><p>A <code>MieBatchWorkspace</code> (grow-only buffers, keyed on backend VALUE + FT + reduction type, single-owner) lets repeated calls across many wavelengths reuse device allocations even though group membership changes from one wavelength to the next (group boundaries depend on the wavenumber-scaled size parameter, so they are NOT fixed across a spectral loop). Note: the backend-VALUE key does not distinguish CUDA devices/streams (KA backend structs are <code>isbits</code> values that don&#39;t encode which physical GPU is active) — single-device, single-stream usage only; see <code>MieBatchWorkspace</code>&#39;s docstring.</p><p>Benchmark on this host (A100, synthetic 882-ensemble × 96-node inventory mirroring the real GCHPIO TOMAS-wet-range column, ~130+ distinct <code>nmax</code> groups):</p><table tabindex="0"><thead><tr><th style="text-align:right;">Path</th><th style="text-align:right;">Time</th></tr></thead><tbody><tr><td style="text-align:right;">CPU, 16-thread <code>Threads.@threads</code> (existing ensemble-level path)</td><td style="text-align:right;">~1.6 s</td></tr><tr><td style="text-align:right;">KA-CPU grouped batched (portable backend, no GPU hardware)</td><td style="text-align:right;">~1.2 s</td></tr><tr><td style="text-align:right;">A100, grouped batched (<code>NativeFloat64</code>)</td><td style="text-align:right;">~0.34 s</td></tr><tr><td style="text-align:right;">A100, serial single-call-per-ensemble loop (pre-batching baseline)</td><td style="text-align:right;">~1.7 s</td></tr><tr><td style="text-align:right;">A100, grouped batched (<code>DSEmulated</code>)</td><td style="text-align:right;">~0.34 s</td></tr><tr><td style="text-align:right;">A100, grouped batched (<code>NativeFloat32</code>)</td><td style="text-align:right;">~0.23 s</td></tr></tbody></table><p>i.e. ~4.9× over the serial single-call CUDA loop and ~4.6× over the 16-thread CPU path on this synthetic column; see <code>test/test_mie_nodes_batched.jl</code>&#39;s Gate 5 benchmark testset for the exact reproduction recipe and group histogram. These numbers are upstream signal only — the actual go/no-go bar is judged against the real GCHPIO column once wired in.</p><p>The CPU path for the batched API does not itself group ensembles (grouping is a GPU-launch-overhead concern); it dispatches to the same <code>Threads.@threads</code> ensemble-level loop as the non-batched CPU path.</p><h2 id="CPU-fallback" tabindex="-1">CPU fallback <a class="header-anchor" href="#CPU-fallback" aria-label="Permalink to &quot;CPU fallback {#CPU-fallback}&quot;">​</a></h2><p><code>CPU()</code> is always available and is the safe default for debugging scenes and for any workflow not yet validated on a GPU backend (Metal Jacobians). Pass <code>architecture=CPU()</code> to <code>make_mie_model</code> to force CPU Mie regardless of the RT solver&#39;s own architecture. The local GPU test suite (<code>test/local/gpu/</code>, not part of CI) self-skips cleanly when no CUDA device is present.</p><h2 id="Performance-Notes" tabindex="-1">Performance Notes <a class="header-anchor" href="#Performance-Notes" aria-label="Permalink to &quot;Performance Notes {#Performance-Notes}&quot;">​</a></h2><p>The main architecture switch is the <code>radiative_transfer.architecture</code> field in the scene configuration, or <code>params.architecture = GPU()</code> after loading a configuration. GPU runs are most useful when enough spectral points, layers, or viewing geometries are batched to amortize kernel-launch and transfer costs. Use <code>CPU()</code> for small debugging scenes and for workflows that are not yet GPU-safe.</p><p>For Apple Silicon experiments, load Metal.jl and use <code>MetalGPU()</code> with <code>Float32</code> scene parameters. The current Metal path focuses on the core batched matrix multiply and inverse operations; fused kernels can be added after Mac validation. The portable inverse kernel uses Metal threadgroup memory and is intended for modest stream/Stokes dimensions; larger matrices fail early with a clear local-memory error instead of a driver launch failure. With the current 32 KiB guard, Float32 matrices with <code>N = Nquad * nStokes &gt;= 64</code> are rejected. Metal Jacobian workflows have not been validated yet, so use CPU or CUDA for linearized runs.</p>',36)])])}const g=t(i,[["render",s]]);export{u as __pageData,g as default};

import{_ as a,o as s,c as o,aB as i}from"./chunks/framework.k9Y_cM_f.js";const u=JSON.parse('{"title":"radiative_transfer block","description":"","frontmatter":{},"headers":[],"relativePath":"pages/IO/Schema/radiative_transfer.md","filePath":"pages/IO/Schema/radiative_transfer.md","lastUpdated":null}'),t={name:"pages/IO/Schema/radiative_transfer.md"};function n(l,e,d,r,c,p){return s(),o("div",null,[...e[0]||(e[0]=[i(`<h1 id="radiative_transfer-block" tabindex="-1"><code>radiative_transfer</code> block <a class="header-anchor" href="#radiative_transfer-block" aria-label="Permalink to &quot;\`radiative_transfer\` block {#radiative_transfer-block}&quot;">​</a></h1><p>Top-level configuration for the RT solver — quadrature, polarization, truncation, and the projection-cap knobs introduced in v0.7. The full file at <code>src/CoreRT/DefaultParameters.yaml</code> shows a representative config; use this page to look up what each field does, what the defaults are, and which legacy aliases still work.</p><h2 id="Required-fields" tabindex="-1">Required fields <a class="header-anchor" href="#Required-fields" aria-label="Permalink to &quot;Required fields {#Required-fields}&quot;">​</a></h2><ul><li><p><strong><code>spec_bands</code></strong> — <code>Vector{String}</code>. Each string is a Julia range expression in cm⁻¹, e.g. <code>&quot;(1e7/777):0.015:(1e7/757)&quot;</code>. Multiple bands run as a band-batched solve.</p></li><li><p><strong><code>surface</code></strong> — <code>Vector{String}</code>. One BRDF constructor per spectral band. See <a href="./surface"><code>Schema/surface.md</code></a> for the surface vocabulary (<code>LambertianSurfaceScalar</code>, <code>CoxMunkSurface</code>, <code>rpvSurfaceScalar</code>, etc.).</p></li><li><p><strong><code>polarization_type</code></strong> — <code>String</code>. One of <code>&quot;Stokes_I()&quot;</code>, <code>&quot;Stokes_IQ()&quot;</code>, <code>&quot;Stokes_IQU()&quot;</code>, <code>&quot;Stokes_IQUV()&quot;</code>. Sets the Stokes vector dimension (1/2/3/4 channels respectively).</p></li><li><p><strong><code>Δ_angle</code></strong> — <code>Real</code>, <strong>optional</strong> (default <code>0.0</code>). Forward-peak exclusion angle in degrees, consumed by <code>δBGE</code> truncation. Ignored when <code>truncation: NoTruncation()</code> or when <code>truncation: auto</code> resolves to <code>NoTruncation()</code>. Legacy YAMLs commonly set <code>2.0</code> to reproduce VLIDORT&#39;s <code>DO_DELTAM_SCALING</code> default — set explicitly when matching a benchmark that pins this value.</p></li><li><p><strong><code>depol</code></strong> — <code>Real</code>. Rayleigh depolarization factor (0 = no depolarization, the Natraj-2009 idealization).</p></li><li><p><strong><code>float_type</code></strong> — <code>String</code>. <code>&quot;Float64&quot;</code> or <code>&quot;Float32&quot;</code>. Float32 is fine for most retrievals and gives a ~2× speedup on large spectral windows; Float64 is required for reproducing published benchmarks at full precision.</p></li><li><p><strong><code>architecture</code></strong> — <code>String</code>. <code>&quot;Architectures.CPU()&quot;</code>, <code>&quot;Architectures.GPU()&quot;</code>, or <code>&quot;Architectures.MetalGPU()&quot;</code>. The CUDA GPU path is loaded via the <code>vSmartMOMCUDAExt</code> weak-dependency extension (<code>using CUDA</code> activates it).</p></li></ul><h2 id="Resolution-knobs-Phase-D-—-v0.7" tabindex="-1">Resolution knobs (Phase D — v0.7) <a class="header-anchor" href="#Resolution-knobs-Phase-D-—-v0.7" aria-label="Permalink to &quot;Resolution knobs (Phase D — v0.7) {#Resolution-knobs-Phase-D-—-v0.7}&quot;">​</a></h2><p>Pick <strong>one</strong> of the schemas below. The new <code>nstreams</code> knob is the recommended path; legacy <code>max_m</code> + <code>l_trunc</code> configs keep working.</p><h3 id="New-schema-recommended" tabindex="-1">New schema (recommended) <a class="header-anchor" href="#New-schema-recommended" aria-label="Permalink to &quot;New schema (recommended) {#New-schema-recommended}&quot;">​</a></h3><ul><li><p><strong><code>nstreams</code></strong> — <em>optional, default <code>8</code></em>. <strong>Weighted streams per hemisphere</strong>, the primary user-facing resolution knob. Public contract: <code>stream_l_cap = 2·nstreams - 1</code> regardless of <code>quadrature_type</code>. For VLIDORT users: this is the Julia equivalent of VLIDORT&#39;s <code>NSTREAMS</code>.</p></li><li><p><strong><code>m_max</code></strong> — <em>optional, default <code>null</code></em>. Explicit Fourier loop bound (order). When set, clamps the trait-aggregator output below <code>stream_l_cap</code>. Useful when you want to truncate the loop more aggressively than the per-component traits would.</p></li><li><p><strong><code>truncation</code></strong> — <em>optional, default <code>auto</code></em>. Controls how the aerosol phase function is treated relative to the projection cap:</p><ul><li><p><code>&quot;auto&quot;</code> <em>(default)</em>: VLIDORT-<code>DO_DELTAM_SCALING</code>-style. With no aerosols, resolves to <code>NoTruncation()</code>. With aerosols, picks <code>δBGE(stream_l_cap, Δ_angle)</code> and the per-band Mie loop applies it only when <code>length(greek.β) &gt; stream_l_cap</code> — short phase functions automatically fall back to <code>NoTruncation()</code> so the truncation step can never crash on a fits-the-cap series. Logs the choice via <code>@info</code>.</p></li><li><p><code>&quot;NoTruncation()&quot;</code> or <code>null</code>: exactly no transform. Coefficients above <code>stream_l_cap</code> are silently dropped from the Fourier projection (per Phase C trait clamps).</p></li><li><p><code>&quot;δBGE(L, Δ)&quot;</code>: explicit δ-BGE-fit truncation (Sanghavi &amp; Stephens 2015). Used for benchmark reproducibility when you want exact parity with a published numerical convention.</p></li></ul></li></ul><h3 id="Legacy-schema-still-supported,-deprecated-v0.7" tabindex="-1">Legacy schema (still supported, deprecated v0.7) <a class="header-anchor" href="#Legacy-schema-still-supported,-deprecated-v0.7" aria-label="Permalink to &quot;Legacy schema (still supported, deprecated v0.7) {#Legacy-schema-still-supported,-deprecated-v0.7}&quot;">​</a></h3><ul><li><p><strong><code>max_m</code></strong> — <code>Integer</code>. Number of Fourier moments (count, loop runs <code>m = 0:max_m-1</code>). Triggers legacy parsing — <code>nstreams</code> will be left unset and the old <code>min(ceil((l_max+1)/2), max_m)</code> aggregator semantics apply.</p></li><li><p><strong><code>l_trunc</code></strong> — <code>Integer</code>. Legendre cutoff. Plays the role of <code>stream_l_cap</code> under the legacy schema.</p></li></ul><p>Setting <strong>both</strong> <code>nstreams</code> and <code>l_trunc</code> produces a <code>@warn</code> — the legacy <code>l_trunc</code> is ignored and <code>nstreams</code> wins.</p><h2 id="Other-optional-fields" tabindex="-1">Other optional fields <a class="header-anchor" href="#Other-optional-fields" aria-label="Permalink to &quot;Other optional fields {#Other-optional-fields}&quot;">​</a></h2><ul><li><p><strong><code>quadrature_type</code></strong> — <em>optional, default <code>&quot;GaussLegQuad()&quot;</code></em>. The recommended default. <code>&quot;RadauQuad()&quot;</code> is supported but documented as expert/legacy: it includes the SZA as a quadrature node (handy for DNI-on-node configurations) at the cost of being 5–50× less accurate per stream than Gauss-Legendre on Rayleigh-only setups (see the Natraj-2009 commit <code>f9403eb</code> and the v0.7 release notes). Sanghavi&#39;s guidance: keep Radau as an option, but reach for Gauss by default.</p></li><li><p><strong><code>numerics</code></strong> — <em>optional</em>. Sub-block with rarely-touched solver knobs:</p><ul><li><p><code>dτ_max_threshold</code> <em>(default <code>0.001</code>)</em> — doubling-step cap. Sets <code>dτ_max = threshold · μ_min</code> where μ_min is the smallest TRUE quadrature stream. Smaller → finer initial layers / more doublings; raising to ~0.01 reduces doublings.</p></li><li><p><code>dτ_min_floor</code> <em>(default <code>1024·eps(FT)</code>)</em> — absolute floor on <code>dτ_max</code>. ~<code>1.2e-4</code> for Float32 / <code>2.3e-13</code> for Float64. Prevents grazing-VZA configs from collapsing dτ below FT precision.</p></li><li><p><code>blas_threads</code> <em>(default <code>null</code>)</em> — per-model BLAS thread cap. <code>null</code> leaves <code>BLAS.get_num_threads()</code> alone; an integer pins it for the duration of <code>rt_run</code>. See commit <code>37390d2</code>.</p></li><li><p><code>verbose</code> <em>(default <code>false</code>)</em> — when <code>true</code>, <code>rt_run</code> prints the <code>TimerOutputs</code> timing tree at the end of each call (useful for profiling, noisy in batched loops).</p></li></ul></li></ul><h2 id="Example-—-minimal-new-schema" tabindex="-1">Example — minimal new schema <a class="header-anchor" href="#Example-—-minimal-new-schema" aria-label="Permalink to &quot;Example — minimal new schema {#Example-—-minimal-new-schema}&quot;">​</a></h2><p>A new-schema config can omit <code>nstreams</code> (default <code>8</code>), <code>quadrature_type</code> (default <code>GaussLegQuad</code>), and <code>truncation</code> (default <code>auto</code>). The leanest valid <code>radiative_transfer</code> block is:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">radiative_transfer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  spec_bands</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;[18867.92 18868.92]&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  surface</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">LambertianSurfaceScalar(0.0)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  polarization_type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Stokes_IQU()</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  Δ_angle</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.0</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  depol</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.0</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  float_type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Float64</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  architecture</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Architectures.CPU()</span></span></code></pre></div><p>This expands to: <code>nstreams = 8</code> (so <code>stream_l_cap = 15</code>), <code>quadrature_type = GaussLegQuad()</code>, <code>truncation = auto</code>. For a Rayleigh-only Lambertian scene this resolves to <code>NoTruncation()</code> and the Fourier loop runs <code>m = 0:2</code> (Phase C trait dispatch).</p><h2 id="Example-—-Cox-Munk-ocean-retrieval" tabindex="-1">Example — Cox-Munk ocean retrieval <a class="header-anchor" href="#Example-—-Cox-Munk-ocean-retrieval" aria-label="Permalink to &quot;Example — Cox-Munk ocean retrieval {#Example-—-Cox-Munk-ocean-retrieval}&quot;">​</a></h2><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">radiative_transfer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  spec_bands</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;[18867 18870]&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  surface</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;CoxMunkSurface(wind_speed=5.0)&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  polarization_type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Stokes_IQU()</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  nstreams</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">16</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          # 16 weighted streams ⇒ stream_l_cap = 31</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  truncation</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">auto</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      # auto → δBGE(31, 2.0) once Mie aerosol is added</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  Δ_angle</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.0</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  depol</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.0</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  float_type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Float64</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  architecture</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Architectures.CPU()</span></span></code></pre></div><h2 id="See-also" tabindex="-1">See also <a class="header-anchor" href="#See-also" aria-label="Permalink to &quot;See also {#See-also}&quot;">​</a></h2><ul><li><p><a href="./surface"><code>Schema/surface.md</code></a> — BRDF vocabulary</p></li><li><p><a href="./aerosols"><code>Schema/aerosols.md</code></a> — Mie size distribution + refractive index</p></li><li><p><a href="./sources"><code>Schema/sources.md</code></a> — <code>SolarBeam</code>, <code>SurfaceSIF</code>, <code>BlackbodySource</code></p></li><li><p><a href="./../../conventions"><code>docs/src/pages/conventions.md</code></a> §6 — <code>Nstreams</code> vs <code>Nquad</code> distinction</p></li><li><p><a href="https://github.com/RemoteSensingTools/vSmartMOM.jl/blob/main/docs/dev_notes/fourier_stream_resolution_plan.md" target="_blank" rel="noreferrer"><code>docs/dev_notes/fourier_stream_resolution_plan.md</code></a> — design memo for the v0.7 schema migration</p></li></ul>`,21)])])}const k=a(t,[["render",n]]);export{u as __pageData,k as default};
